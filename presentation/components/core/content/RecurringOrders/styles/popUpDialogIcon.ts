import { SxProps, Theme } from '@mui/material';

export const recurringOrdersPopUpDialogIconSX: SxProps<Theme> = (theme: Theme) => ({
	color: theme.palette.secondary.main,
	cursor: 'pointer',
	float: 'right',
});
